import React from 'react';
import { Text, StyleSheet } from 'react-native';

const StorageScreen = ({}) => {
  return <>
    <Text style={styles.text}>StorageScreen</Text>
  </>
};

const styles = StyleSheet.create({
  text: {
    fontSize: 20,
  }
});

export default StorageScreen;
